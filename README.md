# -API-Security-Scanner
A Python tool that scans a public API for common security misconfigurations — missing authentication, leaky error messages, exposed hidden endpoints, and missing rate-limiting.
